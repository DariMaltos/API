//diagonal,asterisco, (tab +)
/**
 * Función que calcula la multiplicación de dos números.
 * @param {number} a - Primer número.
 * @param {number} b - Segundo número.
 * @returns {number} El resultado de la multiplicación.
 */

export function multiplicar(a, b) {
  return a * b;
}

//instalarle plugins temas
//incluir un read me
// y cambiar nombre de la carpeta out a doc o algo asi!